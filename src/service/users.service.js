const userService = {
  findAll: async () => {
    return [];
  },
  findOne: async (id) => {
    return {};
  },
  create: async (user) => {
    return user;
  },
  update: async (id, newUser) => {
    return newUser;
  },
  delete: async (id) => {
    return id;
  },
};

export default userService;
